/**
 * Legacy security types placeholder.
 *
 * All security-related domain types now live in the shared `src/types` module.
 * This stub remains only to avoid breaking deep imports during the cleanup phase and
 * will be removed once dependent code is fully migrated.
 */

export {};
